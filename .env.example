# Database
DATABASE_URL=your_neon_database_url_here

# Session Secret
SESSION_SECRET=your_session_secret_here

# Optional: platform will usually inject PORT. Only set locally if needed.
# PORT=5000

# Environment (don't set NODE_ENV in production; your platform will)
NODE_ENV=development
